nelmio_api_doc:
    documentation:
        info:
            title: FlowDog Order API
            description: Recruitment task API for creating orders with validation, pricing and coupons.
            version: 1.0.0
        tags:
            - name: Orders
              description: Order creation endpoints
        servers:
            - url: '%env(DEFAULT_URI)%'
    areas:
        default:
            path_patterns:
                - ^/orders$
                - ^/api/doc
